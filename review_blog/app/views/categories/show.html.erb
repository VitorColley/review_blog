<h1><%= @category.name %></h1>
<h2>Items</h2>
<ul>
  <% @items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %></li>
  <% end %>
</ul>
<%= link_to "New Item", new_item_path %>